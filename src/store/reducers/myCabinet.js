// import {} from '../actions/actionTypes';
const initialState = {
    smth: null
};


export default function myCabinetReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}