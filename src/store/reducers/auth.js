// import {} from '../actions/actionTypes';
const initialState = {
    token: null,
    smth: 'smth'
};


export default function authReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}